# Task 07 — Build Loop (paired process eval)

Build a command-line "expense tracker" as a small Python project. Requirements:

- Add an expense (amount, category, date, note)
- List expenses, with optional filter by category and by month
- Show a summary: total per category, and grand total, for a given month
- Persist expenses to a local file (survives between runs)
- A simple CLI entry point tying it together

Deliver working code. You have one work session. Show your process as you go.
