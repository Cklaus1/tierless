# Task 06 — Decompose

You need to add "password reset via email" to an existing web app that already has
email/password login (users table, session auth, an email-sending helper already
exist). Break this work into an implementation plan you could hand to someone to
execute step by step.
